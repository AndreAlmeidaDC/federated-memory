.claude/
!template/.claude/
!template/.claude/**
*.local

# PDFs gerados localmente; distribuidos via GitHub Releases
releases/

# Dependências do build de PDF (Puppeteer instalado ad-hoc)
node_modules/
package-lock.json
package.json

# Locks de sessão de agentes — apenas o .example é versionado
**/SESSION.lock

